# تاریخچه تغییرات

تمام تغییرات مهم **KevinNet DNS** در این فایل ثبت می‌شود.

این پروژه فقط دو نسخه عمومی منتشر می‌کند:

- **نسخه 3.2.2** — نسخه پایدار اولیه. هنوز در دسترس و قابل استفاده است.
- **نسخه 4.1.4** — نسخه فعلی. برای همه کاربران توصیه می‌شود.

نسخه‌های میانی روی گیت‌هاب وجود ندارد. تکرارهای داخلی قبلی (3.3.x، 3.4.x،
4.0.x، 4.1.0، 4.1.1، 4.1.2، 4.1.3) جایگزین شده‌اند و دیگر منتشر نمی‌شوند.

فرمت از [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) پیروی می‌کند،
و شماره نسخه‌ها از [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [4.1.4] — ۲۰۲۶/۰۵/۲۵

یک release رفع باگ روی 4.1.3.

### رفع شده

- **جدول نتایج دیگر وقتی بعد از اسکن زبان را تغییر می‌دهید پاک نمی‌شود.**

  رفتار قبلی: بعد از اجرای اسکن و گرفتن resolverهای تأیید شده، کلیک
  روی toggle زبان (English ↔ فارسی) جدول نتایج را به طور بصری پاک
  می‌کرد، حتی اگر resolverها هنوز در حافظه بودند (Save و Export
  همچنان کار می‌کردند، اما لیست دیگر قابل مشاهده نبود).

  علت ریشه: refresh زبان پنل اسکنر را از ابتدا rebuild می‌کند تا
  مطمئن شود تمام عنوان کارت‌ها، برچسب دکمه‌ها و متن راهنما زبان جدید
  را می‌گیرند. rebuild یک widget Treeview جدید می‌ساخت و آن را از
  نتایج اسکن موجود repopulate نمی‌کرد.

  رفع: refresh زبان حالا قبل از destroy کردن widgetهای قدیمی،
  snapshot از ردیف‌های فعلی Treeview (با مقادیر و رنگ‌های tag) و
  محتوای log فعالیت می‌گیرد، سپس آن‌ها را بعد از rebuild در ردیف‌های
  جدید بازیابی می‌کند. متن badge و state دکمه Save/Export هم بر اساس
  `_found_ips` re-sync می‌شوند، پس summary پس از اسکن در طول
  toggleهای زبان قابل مشاهده می‌ماند.

### کاربران متأثر

هر کسی که یک اسکن اجرا کرده و سپس زبان رابط را در حالی که نتایج
هنوز روی صفحه بودند تغییر داده. داده در واقع هرگز از دست نرفت — Save
به MasterDNS/VayDNS Profiles و Export DNS List همچنان کار می‌کرد —
اما عدم بازخورد بصری گیج کننده بود.

---

## [4.1.3] — ۲۰۲۶/۰۵/۲۵

یک release رفع باگ روی 4.1.2 که مشکل نمایش متن فارسی فقط روی ویندوز
را حل می‌کند.

### رفع شده

- **متن فارسی/عربی حالا روی ویندوز درست نمایش داده می‌شود.**

  رفتار قبلی روی ویندوز: برچسب‌های فارسی در سراسر UI (نام تب‌ها مثل
  «اسکنر»، عنوان کارت‌ها مثل «موتور VPN» و «اتصال»، متن دکمه‌ها مثل
  «شروع اسکن»، متن وضعیت «آماده» و غیره) به صورت حروف جدا و غیر متصل
  در ترتیب معکوس ظاهر می‌شدند. حروف به هم نمی‌چسبیدند و ترتیب نمایش
  اشتباه بود.

  علت ریشه: UI کارت-محور جدید معرفی شده در 4.1.0 متن فارسی را
  مستقیماً به widgetهای Tk می‌نوشت و pipeline موجود
  `arabic_reshaper + python-bidi` را دور می‌زد. روی macOS، renderer
  بومی CoreText متن خط عربی را بدون کمک درست shape می‌کند. روی ویندوز
  (GDI) و لینوکس (FreeType)، Tk نیاز دارد متن از پیش به ترتیب بصری
  shape شده باشد. مسیر کد قدیمی 3.2.2 این pipeline را همه جا استفاده
  می‌کرد؛ helperهای UI جدید (`make_card`، `field`، `make_seg`،
  `primary_btn`، `secondary_btn`، `save_btn`، برچسب‌های تب، نوار
  وضعیت، header های RESULTS/ACTIVITY، راهنماهای حالت، toastها،
  دیالوگ‌ها) همه آن را از دست داده بودند.

  با wrap کردن هر سایت متن فارسی با `_bidi()` رفع شد، که روی macOS
  no-op است (CoreText shaping را مدیریت می‌کند) و
  `arabic_reshaper + python-bidi` را روی ویندوز و لینوکس اعمال می‌کند.
  این رفع تقریباً ۳۰ سایت widget را تحت تأثیر قرار می‌دهد.

- همچنین فراخوانی‌های `.upper()` روی برچسب‌های فارسی حذف شد (مثلاً
  «موتور VPN»، «اتصال») — فارسی شکل uppercase ندارد، و فراخوانی
  `.upper()` می‌توانست directional control mark ها را strip کند.
  برچسب‌های انگلیسی همچنان `.upper()` می‌گیرند برای ظاهر small-caps
  طراحی.

### کاربران متأثر

هر کسی که KevinNet را روی ویندوز با رابط فارسی پیش‌فرض اپ اجرا می‌کند.
کاربران macOS تحت تأثیر نبودند.

اگر روی ویندوز با 4.1.0، 4.1.1 یا 4.1.2 هستید و متن فارسی خراب به نظر
می‌رسد، به 4.1.3 ارتقا دهید. به عنوان workaround در buildهای قدیمی،
تغییر رابط به انگلیسی از طریق toggle زبان دور باگ را می‌گرفت (متن
انگلیسی تحت تأثیر نیست).

---

## [4.1.2] — ۲۰۲۶/۰۵/۲۵

یک release رفع باگ روی 4.1.1.

### رفع شده

- **ذخیره بعد از اسکن دیگر با «هیچ Resolver یافت نشد» شکست نمی‌خورد
  وقتی کاربر بین حالت MasterDNS و VayDNS toggle می‌کند.**

  رفتار قبلی: تغییر موتور VPN با segmented control بی‌سروصدا نتایج اسکن
  در حافظه را پاک می‌کرد، پس کلیک Save بعد از آن «هیچ Resolver یافت نشد»
  نشان می‌داد حتی با اینکه log همچنان خط verification موفق را نمایش
  می‌داد («مرحله ۳ کامل: ۶۹ resolver تأیید E2E شده، آماده ذخیره»).

  با حذف reset غیرشرطی روی تغییر mode رفع شد. نتایج اسکن حالا در طول
  toggleهای mode persist می‌شوند، و دکمه save مناسب برای هر موتوری که
  فعال است enable می‌شود. این همچنین به کاربران اجازه می‌دهد یک اسکن
  را تحت هر دو پروفایل MasterDNS و VayDNS ذخیره کنند، که چند کاربر
  درخواست کرده بودند.

  راه حل موقت در نسخه‌های قدیمی: اگر این مشکل را در 4.1.1 یا 4.1.0
  دیدید، قبل از تغییر mode اسکن را دوباره اجرا کنید، یا به 4.1.2
  ارتقا دهید.

- دکمه trash/پاک کردن همچنان همه چیز را (نتایج، tree، log، دکمه‌ها)
  مثل قبل reset می‌کند — این راه صریح برای شروع تازه است.

---

## [4.1.1] — ۲۰۲۶/۰۵/۲۲

یک release نگهداری روی 4.1.0. فهرست زیر تمام تغییرات بین v3.2.2 و
v4.1.1 را پوشش می‌دهد، از دیدگاه یک کاربر که از 3.2.2 ارتقا می‌کند.

اگر قبلاً روی 4.1.0 بودید، تنها موارد جدید در 4.1.1 عبارتند از:

- دکمه‌های نوار بالا (راهنما، theme، زبان) و انتخابگر حالت VPN حالا
  در حین اجرای اسکن غیرفعال می‌شوند، تا تصادفاً اسکن در حال اجرا را
  خراب نکنید
- وقتی اسکن DoH/DoT با نتایج تمام می‌شود، یک دیالوگ راهنمای یک‌باره
  توضیح می‌دهد که دکمه «Save to VayDNS Profiles» موجود برای DoH/DoT
  هم کار می‌کند — برنامه به طور خودکار پروفایل‌های `-DoH` و `-DoT`
  جداگانه می‌سازد. با «دفعه بعد نشان نده» قابل dismiss است

بقیه ورودی زیر چیزی است که اگر از 3.2.2 می‌آیید به دست می‌آورید.

یک release اصلی. این نسخه برای همه توصیه می‌شود.

اگر از نسخه 3.2.2 استفاده می‌کنید، اینجا چیزی است که با ارتقا واقعاً
به دست می‌آورید، به زبانی که به عنوان یک کاربر برایتان مهم است.

### چه چیزی برای شما جدید است

**رابط کاربری بازطراحی شده**
کل برنامه ظاهر جدیدی پیدا کرد — پنل‌های تمیزتر با چیدمان کارت مناسب،
یک نشان (لوگو) در گوشه بالا چپ، یک نوار رنگی کنار هر بخش، و یک تب
فعال با خط زیر برای اینکه ببینید کجا هستید. طراحی جدید به یک برنامه
دسکتاپ مدرن نزدیک‌تر است و کمتر مثل یک اسکریپت با پنجره چسبیده به آن
به نظر می‌رسد.

**حالت روشن (Light theme) با تشخیص خودکار**
حالا یک آیکون خورشید/ماه در نوار بالا وجود دارد. روی آن کلیک کنید تا
بین حالت تاریک و روشن جابجا شوید. کاربران جدید همان حالتی را می‌بینند
که سیستم‌عاملشان تنظیم کرده (حالت تاریک macOS، تنظیم روشن/تاریک
Windows Apps، طرح رنگی GNOME در لینوکس). پس از تغییر، یک بار برنامه
را restart کنید تا تغییر همه جا اعمال شود.

**حالت اسکن جدید: DoH/DoT (endpointهای رمزنگاری شده)**
ترافیک معمولی UDP/53 توسط DPI ایران شناسایی می‌شود. KevinNet حالا
می‌تواند endpointهای عمومی DNS-over-HTTPS (پورت 443) و DNS-over-TLS
(پورت 853) را اسکن کند. این ترافیک شبیه HTTPS معمولی است و تشخیص آن
به عنوان ترافیک تونل بسیار سخت‌تر است. دکمه جدید «Scan DoH/DoT» فقط
در حالت VayDNS قابل مشاهده است (MasterDNS از این انتقال‌ها پشتیبانی
نمی‌کند). ۲۵ endpoint برای DoH و ۱۸ endpoint برای DoT از پیش تنظیم
شده عرضه می‌شوند.

**ذخیره نتایج DoH/DoT مستقیماً در پروفایل‌های VayDNS**
پس از یک اسکن موفق DoH/DoT، روی همان دکمه «ذخیره در VayDNS Profiles»
که برای اسکن معمولی UDP استفاده می‌کنید کلیک کنید. اگر اسکن شما هم
endpoint های DoH و هم DoT را پیدا کرده باشد، برنامه به‌طور خودکار دو
پروفایل می‌سازد — یکی با پسوند `-DoH` و دیگری با پسوند `-DoT`. یک
دیالوگ راهنمای یک‌باره در اولین مرتبه ظاهر می‌شود که نحوه کار را
توضیح می‌دهد.

**دو موتور VPN، با جدایی تمیز**
انتخابگر حالت (MasterDNS / VayDNS) حالا یک کنترل segmented است که نیمه
فعال با رنگ حالت بالا می‌آید (آبی برای MasterDNS، بنفش برای VayDNS).
فیلدها و دکمه‌ها به طور خودکار تغییر شکل می‌دهند تا فقط چیزی که مرتبط
است دیده شود. دکمه اسکن DoH/DoT وقتی MasterDNS انتخاب شده ناپدید می‌شود.

**راست‌کلیک روی لیست Resolverها**
روی هر IP در نتایج راست‌کلیک کنید (یا دو-انگشت کلیک / Ctrl-کلیک روی
macOS) تا یک منو بگیرید: کپی IP، کپی همه IP ها، باز کردن پوشه خروجی
در فایل منیجر.

**نشانگر آخرین اجرا روی پروفایل‌ها**
هر پروفایل ذخیره شده «۵ دقیقه پیش اجرا شد»، «۳ ساعت پیش اجرا شد» و
غیره را در لیست نشان می‌دهد — مشخص می‌کند کدام پروفایل در حال حاضر فعال
است.

**ردیف‌های پروفایل که اسم‌های شما را جا می‌دهند**
اسم‌های طولانی پروفایل قبلاً از سمت راست کوتاه می‌شدند. حالا به خط دوم
می‌روند (wrap می‌شوند). لیست همچنین عریض‌تر است، بنابراین بیشتر اسم‌ها
در همان خط اول جا می‌شوند.

**یک صفحه راهنمای واقعی**
روی دکمه `?` کلیک کنید. مرورگر شما با یک صفحه راهنمای تمیز (با theme
مطابق برنامه) باز می‌شود که workflow، گزینه‌ها و عیب‌یابی را پوشش
می‌دهد — کپی-پیست دستورات راه‌اندازی سرور از آن خیلی راحت‌تر از دیالوگ
قدیمی داخل برنامه است. آفلاین کار می‌کند؛ اگر screenshot بگیرید و روی
موبایل ببینید هم خوب است.

**برنامه چیزی را که آخرین بار تایپ کردید به یاد می‌آورد**
دامنه تونل، پوشه خروجی و آخرین حالت VPN استفاده شده وقتی برنامه را
دوباره باز می‌کنید بازیابی می‌شوند، تا مجبور نباشید در هر launch
دوباره تایپ کنید.

**پیام‌های خطای واضح‌تر**
اگر یک دامنه، کلید رمزنگاری یا کلید عمومی نامعتبر پیست کنید، برنامه
قبل از شروع اسکن آن را می‌گیرد و دقیقاً به شما می‌گوید چه اشکالی دارد —
به جای اینکه بعداً با یک «parse TOML failed» گیج‌کننده شکست بخورد.

**دکمه‌ها در حین اسکن قفل می‌شوند**
وقتی اسکن در حال اجرا است، نوار بالا (راهنما، theme، زبان) و انتخابگر
حالت VPN غیرفعال می‌شوند. دیگر نمی‌توانید با کلیک‌های تصادفی اسکن در
حال اجرا را خراب کنید. دکمه توقف فعال می‌ماند — آن راه خروج شماست.

### چه چیزی امن‌تر است

**محافظت بهتر از کلیدهای رمزنگاری شما**
روی Linux و macOS، فایل‌های JSON که کلیدهای رمزنگاری MasterDNS شما را
ذخیره می‌کنند (و فایل `client_config.toml` تولید شده) حالا با مجوزهای
`0600` ساخته می‌شوند. کاربران دیگر روی همان دستگاه دیگر نمی‌توانند
آن‌ها را بخوانند.

**configهای تولید شده دیگر روی ورودی‌های سخت خراب نمی‌شوند**
کوتیشن‌های اضافی، بک‌اسلش‌ها یا کاراکترهای خاص در دامنه یا کلید قبلاً
یک `client_config.toml` خراب تولید می‌کردند. ورودی‌ها حالا به درستی
escape می‌شوند، و اسکریپت launch روی Unix از quote شل امن استفاده
می‌کند.

**اسکن‌های دقیق‌تر**
parser خروجی استفاده شده توسط مرحله تأیید SlipNet حالا سخت‌گیرتر است —
IP های نامعتبر در خروجی tool به جای اینکه به عنوان resolver تلقی شوند،
بی‌سروصدا حذف می‌شوند.

### چه چیزی برای به‌روزرسانی امن‌تر است

**دانلودهای قابل تأیید**
هر release روی گیت‌هاب حالا با یک `SHA256SUMS.txt` در کنار باینری‌ها
عرضه می‌شود. اگر از جایی غیر از صفحه رسمی Releases دانلود می‌کنید،
می‌توانید تأیید کنید که باینری دستکاری نشده:

```bash
shasum -a 256 -c SHA256SUMS.txt    # macOS / Linux
Get-FileHash -Algorithm SHA256 KevinNet_Windows_x64.exe   # Windows
```

**قبل از هر release تست می‌شود**
pipeline ساخت حالا یک مجموعه تست ۹۲ تستی pytest را قبل از تولید هر
باینری پلتفرم اجرا می‌کند. اگر یک تست شکست بخورد، هیچ release ساخته
نمی‌شود — پس کدی که ship می‌شود واقعاً کار می‌کند.

### چه چیزی همان است

- تمام گزینه‌های اصلی که از 3.2.2 می‌شناسید هنوز وجود دارند
- تب‌های MasterDNS Profiles و VayDNS Profiles به همان روش کار می‌کنند
- فرمت JSON پروفایل سازگار با عقب است — پروفایل‌های قدیمی که در 3.2.2
  ذخیره کردید در 4.1.1 درست load و اجرا می‌شوند
- باینری‌های VPN عرضه شده (MasterDnsVPN، vaydns-client) همان نسخه‌های
  upstream هستند

### محدودیت‌های شناخته شده

- **تغییر theme به restart نیاز دارد.** Tkinter یک سیستم theme واقعی
  ندارد — رنگ‌ها در زمان ایجاد به widgetها baked می‌شوند. تلاش برای
  تغییر رنگ زنده روی toggle یک نتیجه نیمه-themed تولید می‌کرد که از
  انتخاب یک طرف بدتر به نظر می‌رسید. toggle حالا ترجیح شما را ذخیره
  می‌کند و یک toast تأیید تغییر نشان می‌دهد؛ یک بار برنامه را restart
  کنید و همه چیز تمیز سوییچ می‌شود.
- **پنل جزئیات پروفایل هنوز از طراحی قدیمی استفاده می‌کند.** فقط ردیف‌های
  لیست در سمت چپ طراحی بصری جدید گرفتند. پنل جزئیات در سمت راست
  عملکردی است اما کمتر از بقیه برنامه polished به نظر می‌رسد. این یک
  تصمیم scope عمدی است تا release را قابل مدیریت نگه دارد — یک port
  کامل برای یک release آینده برنامه‌ریزی شده است.
- **برچسب‌های جزئیات پروفایل با تغییر زبان refresh نمی‌شوند.** برچسب‌های
  آنجا به همان زبانی می‌مانند که وقتی تب اولین بار باز شد فعال بود.
  به تب Scanner برگردید و تب Profiles را دوباره باز کنید تا refresh
  شوند. این یک محدودیت از قبل موجود از v3.2.2 است، نه چیزی که در
  4.1.1 معرفی شده باشد.

### نحوه ارتقا

۱. `KevinNet_<platform-شما>` را از [صفحه Releases](../../releases/latest) دانلود کنید
۲. (توصیه می‌شود) SHA-256 را مقابل `SHA256SUMS.txt` تأیید کنید
۳. باینری جدید را اجرا کنید
۴. پروفایل‌های قدیمی شما از 3.2.2 به طور خودکار load می‌شوند — چیزی
   برای migrate کردن نیست

اگر با 4.1.1 به مشکلی برخوردید، می‌توانید همچنان از v3.2.2 استفاده
کنید (هنوز در صفحه Releases موجود است) در حالی که مشکل را گزارش
می‌دهید.

---

## [3.2.2] — ۲۰۲۶/۰۴/XX

release پایدار قبلی. هنوز کار می‌کند، هنوز در دسترس است.

اگر قبلاً از 3.2.2 استفاده می‌کردید، دقیقاً همان کارهایی را انجام
می‌دهد که همیشه انجام می‌داد: اسکن resolverهای DNS ایرانی، ذخیره
پروفایل‌های MasterDNS یا VayDNS، و launch کلاینت VPN.

**فقط در این صورت 3.2.2 را انتخاب کنید:**

- روی 3.2.2 یک setup کاری دارید و نمی‌خواهید چیزی را تغییر دهید
- روی یک محیط غیرمعمول هستید که 4.1.1 یک regression در آن دارد که
  گزارش داده‌اید و ما هنوز fix نکرده‌ایم

**برای بقیه، از 4.1.1 استفاده کنید.** همان قابلیت‌ها به اضافه موارد
جدید بالا (اسکن DoH/DoT، حالت روشن، رابط بازطراحی شده، مدیریت ورودی
hardened، امنیت بهتر روی Unix) را دارد.

فایل‌های پروفایل ذخیره شده در 3.2.2 در 4.1.1 بدون تغییر کار می‌کنند،
بنابراین ارتقای بعدی بدون ریسک است — می‌توانید بین نسخه‌ها رفت و
برگشت کنید بدون اینکه داده‌هایتان را از دست بدهید.

---
